let a = document.querySelectorAll("a");
let p = document.querySelectorAll("p");
let h = document.querySelectorAll("h1,h2,h3,h4,h5,h6");

console.log(`Hay ${a.length} links, ${p.length} parrafos, ${h.length} encabezados.`)